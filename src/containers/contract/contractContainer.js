import React, { Fragment } from 'react'
import ReactDOM from 'react-dom'

function ContractContainer(props) {
  console.log('in contract container!')

  return <div>Hey bitches</div>
}

export default ContractContainer
